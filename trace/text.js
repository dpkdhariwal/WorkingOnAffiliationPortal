// Get all input elements of type "text"
var inputs = document.querySelectorAll('input[type="text"]');

// Loop through all input elements and set their value
inputs.forEach(function(input) {
    input.value = 'dffaf';
});